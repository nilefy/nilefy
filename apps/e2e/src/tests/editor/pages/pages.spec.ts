import { test } from '../../../fixtures';

test('1. Should be able to create a new page', async ({ editorPage }) => {});

test('2. Should be able to edit a page', async ({ editorPage }) => {});

test('3. Should be able to delete a page', async ({ editorPage }) => {});

test('4. Should be able to go to a page through an action button', async ({
  editorPage,
}) => {});
