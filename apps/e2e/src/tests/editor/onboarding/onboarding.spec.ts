import { test } from '../../../fixtures';

test('1. Should show onboarding first time visiting editor', async ({
  editorPage,
}) => {});

test('2. Should not show onboarding when visiting editor again', async ({
  editorPage,
}) => {});

test('3. Should be able to dismiss onboarding', async ({ editorPage }) => {});

test('4. onboarding steps', async ({ editorPage }) => {});
