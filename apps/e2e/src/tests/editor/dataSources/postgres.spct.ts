import { test } from '../../../fixtures';

test('1. Should be able to add a new database data source', async ({
  editorPage,
}) => {});

test('2. Should be able to test the connection', async ({ editorPage }) => {});

test('3. Should be able to run an arbitrary query', async ({
  editorPage,
}) => {});

test('4. Should be able to use query data', async ({ editorPage }) => {});
