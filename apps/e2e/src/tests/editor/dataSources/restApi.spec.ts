import { test } from '../../../fixtures';

test('1. Should be able to add a new REST API data source', async ({
  editorPage,
}) => {});

test('2. Should be able to use different HTTP methods', async ({
  editorPage,
}) => {});

test('3. Should be able to use query parameters', async ({ editorPage }) => {});

test('4. Should be able to use headers', async ({ editorPage }) => {});

test('5. Should be able to use request body', async ({ editorPage }) => {});

test('6. Should be able to use authentication', async ({ editorPage }) => {});

test('7. Should be able to use response data', async ({ editorPage }) => {});

test('8. Should be able to use query state', async ({ editorPage }) => {});

test("9. Trigger mode: It should be able to run on startup if trigger mode is set to 'On App Load'", async ({
  editorPage,
}) => {});

test("10. Trigger mode: It shouldn't run on startup if trigger mode is set to 'Manual'", async ({
  editorPage,
}) => {});

test('11. Should be able to delete a REST API data source', async ({
  editorPage,
}) => {});

test('12. Should be able to run onSuccess event', async ({ editorPage }) => {});

test('13. Should be able to run onError event', async ({ editorPage }) => {});

test('14. Should be able to run onAuthError event', async ({
  editorPage,
}) => {});
