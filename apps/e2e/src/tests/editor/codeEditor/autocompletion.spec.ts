import { test } from '../../../fixtures';

test('1. Should show autocompletion when typing', async ({ editorPage }) => {});

test('2. Should show autocompletion when pressing Ctrl+Space', async ({
  editorPage,
}) => {});

test('3. Should show platform entities in autocompletion', async ({
  editorPage,
}) => {});

test('4. Should show entity fields in autocompletion', async ({
  editorPage,
}) => {});
