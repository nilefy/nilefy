import { test } from '../../../fixtures';

test('1. Should show default libraries', async ({ editorPage }) => {});

test('2. Should be able to add a library', async ({ editorPage }) => {});

test('3. Should be able to remove a library', async ({ editorPage }) => {});

test('4. Should be able to use a library', async ({ editorPage }) => {});
