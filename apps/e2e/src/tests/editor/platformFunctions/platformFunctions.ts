import { test } from '../../../fixtures';

test('1. Should be able to get user info', async ({ editorPage }) => {});

test('2. Should be able to get current page info', async ({
  editorPage,
}) => {});

test('3. Should be able to alert', async ({ editorPage }) => {});
